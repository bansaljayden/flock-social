# Design System: Flock

> **THIS FILE IS NOT THE SOURCE OF TRUTH, AND HAS NOT BEEN SINCE 2026-08-14.**
> `SLOP-AUDIT.md` wins over this file by Jayden's decision. That precedence is
> recorded at the top of `frontend/public/SLOP-AUDIT.md` and it specifically
> voids two instructions that used to live here: the overshoot easing
> `cubic-bezier(0.34, 1.56, 0.64, 1)`, and the instruction to add `.scroll-fade`
> reveals. Both are banned there. `CONTRIBUTING.md` and the root `README.md`
> both name SLOP-AUDIT as the binding standard and neither mentions this file.
>
> **Three sections below were measured against `src/index.css` and `src/App.js`
> on 2026-08-26 and found wrong.** They are corrected inline and marked. Obeying
> the uncorrected versions would have removed the brand typeface from the
> product, because this file banned the exact face the product ships. Read a
> token out of `index.css` before trusting any value here.

> Originally generated by auditing the
> live tokens in `src/index.css` + `src/App.js` (stitch-design-taste format,
> cross-checked against impeccable / high-end-visual-design / web-design-guidelines).
> Flock is a **product app** (multi-step social coordination for Gen Z), not a
> marketing site — design SERVES the product. Keep it warm, tactile, legible.

## 1. Visual Theme & Atmosphere
Warm, tactile, "soft daylight" consumer app. Density **Daily-App Balanced (4)** —
compact but breathing, never a data cockpit. Variance **Low-offset (3–4)** — a
product UI, so symmetry and predictability aid usability over artsy asymmetry.
Motion **Fluid (5)** — spring-eased micro-interactions, a live "pulse" language,
no cinematic scroll-hijacking. The feel is a cream paper surface under navy ink: friendly, physical (film grain + soft ambient shadows), quietly premium.

## 2. Color Palette & Roles
Light mode (`:root`):
- **Cream Canvas** (`#f1ede0`) — primary app background (`--bg-primary`)
- **Pure Surface** (`#ffffff`) — cards, modals (`--bg-card-solid`)
- **Warm Tertiary** (`#e8e0d5`) — inset wells, input fills (`--bg-tertiary`)
- **Ink** (`#1e293b`) — primary text (`--text-primary`)
- **Slate Secondary** (`#556274`) — secondary text — raised for WCAG AA (`--text-secondary`)
- **Muted Tertiary** (`#78859a`) — meta/hints — raised toward AA (`--text-tertiary`)
- **Steel Navy** (`#2d5a87` light / `#6d9ac3` dark) — THE single brand accent:
  active states, focus rings, badges, distance/map cues (2026-07 redesign: teal killed)
- Semantic only: amber (warnings/moderate), red (danger/busy), green (go/available)

Dark mode (`[data-theme="dark"]`): navy-slate family (`#0f172a` canvas, `#1e293b`
surfaces), steel accent lightened to `#6d9ac3`, muted text lightened for AA.

**Locks & bans:**
- **One accent: steel navy.** No second accent. Purple is fully dead (2026-07):
  Birdie AI, Pro badges, concert category, NIGHT badge all steel/navy family.
- No pure `#000000` / pure `#ffffff` backgrounds (cream + off-navy carry depth).

## 3. Typography Rules
- **Family: Fraunces for display over Hanken Grotesk for body**, both self-hosted
  as variable fonts in `src/fonts/`, so they render offline in the Capacitor
  WebView. **This line said "Satoshi on everything" until 2026-08-26.** Satoshi
  was dropped on 2026-08-12 for being the recognisable AI-startup face
  (`index.css:8`), and `src/fonts/` holds exactly two files, neither of them
  Satoshi. Fraunces is what SLOP-AUDIT §I means by "a font that departs from the
  norm", and typography is the one thing the videos rated 10/10.
- **Hierarchy by weight, not scale:** 800 for screen titles (~20px), 700 for card
  headings, 600 for labels/emphasis, 500 for muted meta, 400 body. Tight tracking
  on titles (`-0.3px` to `-0.4px`).
- **Numbers use `tabular-nums` globally** — stats, crowd %, budgets, timestamps
  align in columns (set on `body`).
- **Banned:** Inter/Roboto/system-default as the visible face; loading fonts from
  a CDN (must be self-hosted/offline). **"Serif anywhere" is NO LONGER a ban and
  was the most damaging line in this file.** `--font-display` is
  `'Fraunces', 'Iowan Old Style', Georgia, serif` (`index.css:136`). The display
  face IS a serif, on purpose, and a contributor enforcing the old ban would
  have deleted the brand type as a violation.

## 4. Component Stylings
- **Buttons:** the REFINED `.glass-*` system (2026-07) — modest blur(10px), 1px
  hairline, single inner highlight, whisper shadow. `.glass-primary` = navy glass
  (light) / cream glass (dark) — the login CTA language. No glow stacks ever.
  Tactile `scale(0.98)` on `:active`; hover = brightness shift, not scale.
- **Cards:** FLAT — rounded 12–16px, `--bg-card-solid`, 1px `--border-default`
  hairline, whisper `--card-shadow-sm`. No blur on cards. No nested cards.
- **Elevation:** whisper shadows only (≤0.05 light); modals may carry a soft
  0.15 black shadow. Never colored glows.
- **Inputs:** label above, error below; readable fill (`--bg-tertiary`), steel focus.
- **Surfaces:** a fixed film-grain overlay (0.022) gives editorial texture.

## 5. Layout Principles
- Mobile-first, single column (this is a phone app in a WebView). `100dvh`-aware.
- Compact header + scrollable content pattern (see `HomeScreen`).
- Bottom tab nav (Nest / Discover / Messages / Profile).
- Consistent 12–16px radii; 8–16px gaps; generous-but-tight padding.

## 6. Motion & Interaction
- Spring easing `cubic-bezier(0.34,1.56,0.64,1)` for tactile pops (FAB, greeting).
- A live **"pulse"** language: gentle 2s occupancy/status pulses on active dots.
- Staggered `.scroll-fade` reveals on lists (IntersectionObserver, not scroll listeners).
- **`prefers-reduced-motion` honored** — infinite orb/shimmer/pulse loops stop,
  transitions collapse, scroll-reveal content forced visible. Non-negotiable a11y.
- Animate only `transform`/`opacity`; grain on a fixed pseudo-element only.

## 7. Anti-Patterns (Banned)
- No second accent color; no indigo/purple "AI gradient".
- No CDN-loaded fonts (offline-first); no Inter/system default as the face.
- No flat bordered cards with no elevation; no nested cards.
- No muted gray text below WCAG AA (`--text-tertiary` floor raised for this reason).
- No animation without a `prefers-reduced-motion` fallback.
- No emojis as UI icons (use the in-app `Icons.*` SVG set).
- No serif; no pure black/white surfaces; no glass added purely for decoration.
